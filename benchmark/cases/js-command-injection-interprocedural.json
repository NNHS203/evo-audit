{
  "schemaVersion": 1,
  "caseId": "js-command-injection-interprocedural-001",
  "split": "development",
  "language": "typescript",
  "file": "src/routes.ts",
  "code": "import { exec } from 'node:child_process';\nfunction runCommand(value) { return exec(value); }\nexport function handler(req) { return runCommand(req.query.command); }\n",
  "property": "A source-to-sink path must survive a helper call boundary.",
  "expected": {
    "vulnerable": true,
    "ruleId": "JS-COMMAND-INJECTION-001",
    "obligationKind": "SOURCE_TO_SINK",
    "minimumReportableEvidence": "T2_REPRODUCIBLE",
    "mustNotClaimSafeFromNoMatch": true
  },
  "negativeControls": ["runCommand('echo fixed')"],
  "notes": ["The AST graph uses a bounded function-parameter summary; it is not a claim of complete interprocedural analysis."]
}
