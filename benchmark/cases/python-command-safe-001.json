{
  "schemaVersion": 1,
  "caseId": "python-command-safe-001",
  "split": "development",
  "language": "python",
  "file": "app.py",
  "code": "import subprocess\n\ndef run():\n    return subprocess.run(['echo', 'fixed'], check=True)\n",
  "property": "A fixed argument array is not a request-controlled command path.",
  "expected": {
    "vulnerable": false,
    "ruleId": "PY-COMMAND-INJECTION-001"
  },
  "notes": [
    "False-positive trap for a command sink with no tracked external input."
  ]
}
