{
  "schemaVersion": 1,
  "caseId": "js-command-injection-basic-001",
  "split": "holdout-example",
  "language": "javascript",
  "code": "export function handler(req, res) { return exec(req.query.command); }",
  "property": "request-controlled input must not reach a shell command sink without a fixed-command boundary",
  "expected": {
    "obligationKind": "SOURCE_TO_SINK",
    "minimumReportableEvidence": "T2_REPRODUCIBLE",
    "mustNotClaimSafeFromNoMatch": true
  },
  "negativeControls": [
    "a fixed command with no request-derived argument",
    "a rejected request value proved by an isolated test"
  ],
  "notes": [
    "This is a protocol example, not a benchmark result.",
    "Real evaluations should include framework variants, helper indirection, guards, and non-vulnerable lookalikes."
  ]
}
