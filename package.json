{
  "name": "evo-audit",
  "version": "0.1.0",
  "description": "CLI-first, evidence-gated AI code auditing core",
  "type": "module",
  "bin": {
    "evo-audit": "dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "start": "node dist/src/cli.js"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
